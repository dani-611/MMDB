import { Typography } from '@mui/material';

export const AllMoviesTitle = () => {
  return (
    <Typography sx={{ fontSize: '1.8rem', fontWeight: '600' }}>
      All Movies
    </Typography>
  );
};
